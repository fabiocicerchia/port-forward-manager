# Basic Example

What it shows: forwarding a Postgres service and an API deployment at once,
with auto-reconnect.

## Run

```sh
portfwd up examples/basic/portfwd.yaml
portfwd status
portfwd down
```
