# Examples

Runnable, self-contained examples. Each subfolder shows one clear use case.

- `basic/` — a minimal profile and the commands to drive it.
- `env/` — generating a project's `.env` from the same profile.
- `protected/` — guarding a production forward behind `--reason`, and the
  local log of every session that opened one.
